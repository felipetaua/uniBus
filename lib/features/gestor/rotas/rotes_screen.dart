import 'package:flutter/material.dart';

class RotesPages extends StatelessWidget {
  const RotesPages({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
