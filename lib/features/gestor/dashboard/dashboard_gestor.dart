import 'package:flutter/material.dart';

class GestorDashboardPage extends StatelessWidget {
  const GestorDashboardPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
